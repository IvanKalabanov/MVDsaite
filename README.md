# МВД Enter Project

Модульная система управления правоохранительной организацией. Проект полностью работает на фронтенде и хранит данные в `localStorage`, поэтому запуск дополнительного бэкенда больше не требуется.

## Установка и запуск

```bash
npm install
npm run dev
```

Приложение откроется на `http://localhost:5173`.

### Производственная сборка

```bash
npm run build
npm run preview
```

## Демо-аккаунты

Эти аккаунты создаются автоматически и доступны «из коробки»:

| Роль | Логин | Пароль |
| --- | --- | --- |
| Администратор | `admin` | `admin123` |
| Руководитель | `leader` | `leader123` |
| Сотрудник | `employee` | `employee123` |
| Гражданин | `user` | `user123` |

Вы также можете зарегистрировать новых пользователей — они попадут в то же локальное хранилище.

## Как хранятся данные

- Все сущности (заявления, новости, сотрудники и т.д.) синхронизируются с `localStorage`.
- Первое открытие подтягивает моковые данные из `src/data/mockData.js`.
- Чтобы полностью сбросить состояние, выполните в консоли браузера:

```js
localStorage.removeItem('mvdsai-local-data');
window.location.reload();
```

## Структура проекта

```
├── src/
│   ├── components/   # UI- и доменные компоненты
│   ├── pages/        # Страницы
│   ├── context/      # Контексты (авторизация)
│   ├── utils/        # Локальный «API» и вспомогательные функции
│   ├── data/         # Моковые данные
│   └── styles/       # Глобальные стили
├── public/
└── package.json
```

## Роли пользователей

- `user` — подача заявлений, просмотр новостей.
- `employee` — доступ к базе данных и сотрудникам.
- `leader` — управление сотрудниками, руководителями и новостями.
- `admin` — полный доступ ко всем разделам и настройкам.

## Возможности

- 📝 Подача заявлений и переписка по ним.
- 👮 Управление сотрудниками и отделами.
- 🗃️ Работа с базой данных нарушителей.
- 👔 Список руководителей и редактирование.
- 📰 Новости и модальные окна для чтения.
- 🔐 Ролевой доступ на уровне интерфейса.

## Технологии

- **React 19** + **React Router 7**
- **Vite 7**
- **Context API** для авторизации
- **CSS** (кастомные стили без UI-библиотек)

## FAQ

**Нужно ли поднимать сервер или БД?**  
Нет. Всё сохраняется в браузере пользователя.

**Можно ли перенести данные?**  
Да. Экспортируйте содержимое `localStorage['mvdsai-local-data']` и импортируйте его на другой машине.

**Почему данные пропали после очистки?**  
Очистка кеша/данных браузера удаляет `localStorage`. Просто перезагрузите страницу — подтянутся моковые данные по умолчанию.
